import { MondayFinderPipe } from './monday-finder.pipe';

describe('MondayFinderPipe', () => {
  it('create an instance', () => {
    const pipe = new MondayFinderPipe();
    expect(pipe).toBeTruthy();
  });
});
